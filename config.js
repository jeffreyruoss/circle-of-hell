// Development Configuration
const DEV_CONFIG = {
  // Set to true to skip text sequence (3 hourz, 1 mile loop, 2much FUn) and jump straight to fire
  skipIntro: false,

  // Set to true to disable video loading/playing
  disableVideo: false,

  // Set to true to disable audio loading/playing
  disableAudio: false,
};

// Export for use in other scripts
window.DEV_CONFIG = DEV_CONFIG;
